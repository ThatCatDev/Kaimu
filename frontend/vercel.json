{
  "buildCommand": "astro build --config astro.config.vercel.mjs",
  "framework": "astro",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "$BACKEND_URL/:path*"
    }
  ]
}
